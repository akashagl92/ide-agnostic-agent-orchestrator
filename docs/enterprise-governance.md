# Enterprise Governance

- Use policy overlays per environment/stage.
- Keep immutable event logs for audits.
- Require approvals before enabling `scoped_write` in production repos.
- Enforce schema validation in CI.
